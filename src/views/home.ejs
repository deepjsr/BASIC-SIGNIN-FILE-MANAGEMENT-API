<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daily Work Management System</title>
    <!-- <link rel="stylesheet" href="/public/stylesheets/styles.css"> -->
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            /* background-image: url('./background.jpg'); */
            background-color: aqua;

            /* Replace with the actual background image if available */
            background-size: cover;
            background-position: center;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #fff;
        }

        .container {
            text-align: center;
            background-color: rgba(0, 0, 0, 0.5);
            /* Semi-transparent background */
            padding: 20px;
            border-radius: 10px;
            width: 90%;
            max-width: 400px;
        }

        header {
            background-color: #FFA000;
            /* Orange color */
            padding: 10px;
            border-radius: 10px 10px 0 0;
        }

        header h1 {
            font-size: 1.5em;
        }

        main {
            padding: 20px;
        }

        main h2 {
            margin-bottom: 20px;
            font-size: 1.2em;
        }

        .buttons {
            display: flex;
            justify-content: space-around;
        }

        .module-btn {
            background-color: #FFA000;
            /* Orange color */
            border: none;
            color: white;
            padding: 10px 20px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 4px 2px;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .module-btn:hover {
            background-color: #FF8C00;
            /* Darker orange on hover */
        }
    </style>
</head>

<body>
    <div class="container">
        <header>
            <h1>Daily Work Management System</h1>
        </header>
        <main>
            <h2>Select Module</h2>
                <div class="buttons">
                    <button class="module-btn"><a href="/admin">Admin</a></button>
                    <button class="module-btn">Approval</button>
                    <button class="module-btn"><a href="/">User</a></button>
                </div>
        </main>
    </div>
</body> 

</html>